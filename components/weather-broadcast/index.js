import styled from "styled-components";

const Wrapper = styled.section`
  width: 100%;
  height: 250px;
`;

const WeatherBroadcast = () => {
  return <Wrapper> weekly broadcast </Wrapper>;
};

export default WeatherBroadcast;
